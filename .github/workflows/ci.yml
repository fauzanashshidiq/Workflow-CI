name: MLflow CI Advanced

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  train-build-push:
    runs-on: ubuntu-latest

    steps:
      - name: Run actions/checkout@v4
        uses: actions/checkout@v4

      - name: Set up Python 3.12.7
        uses: actions/setup-python@v5
        with:
          python-version: "3.12.7"

      - name: Check Env
        run: |
          python --version
          pip --version
          docker --version

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install mlflow==2.19.0 pandas numpy scikit-learn matplotlib joblib PyYAML

      - name: Run mlflow project
        run: mlflow run MLProject --env-manager=local

      - name: Get latest MLflow run_id
        id: latest-run
        run: |
          python - <<'PY'
          from pathlib import Path
          import os
          import yaml

          candidates = []
          for meta_path in Path("mlruns").glob("*/*/meta.yaml"):
              if "models" in meta_path.parts or "datasets" in meta_path.parts:
                  continue
              with meta_path.open() as file:
                  meta = yaml.safe_load(file)
              candidates.append((meta.get("start_time", 0), meta.get("run_id")))

          if not candidates:
              raise SystemExit("Tidak ada MLflow run_id yang ditemukan.")

          run_id = sorted(candidates)[-1][1]
          with open(os.environ["GITHUB_OUTPUT"], "a") as output:
              output.write(f"run_id={run_id}\n")
          print(f"Latest run_id: {run_id}")
          PY

      - name: Install Python dependencies
        run: |
          pip list

      - name: Upload to GitHub Artifact
        uses: actions/upload-artifact@v4
        with:
          name: mlflow-training-artifacts
          path: |
            mlruns/
            MLProject/artifacts/

      - name: Build Docker Model
        run: |
          mlflow models build-docker \
            -m "runs:/${{ steps.latest-run.outputs.run_id }}/model" \
            -n heart-disease-mlflow \
            --enable-mlserver

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Tag Docker Image
        run: |
          docker tag heart-disease-mlflow ${{ secrets.DOCKERHUB_USERNAME }}/heart-disease-mlflow:latest
          docker tag heart-disease-mlflow ${{ secrets.DOCKERHUB_USERNAME }}/heart-disease-mlflow:${{ github.sha }}

      - name: Push Docker Image
        run: |
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/heart-disease-mlflow:latest
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/heart-disease-mlflow:${{ github.sha }}
